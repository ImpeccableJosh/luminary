{
  "name": "luminary-manim-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "DOTENV_CONFIG_PATH=.env tsx --require dotenv/config server.ts",
    "start": "node dist/server.js",
    "build": "tsc --project tsconfig.json && tsc-alias -p tsconfig.json"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.74.0",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^22",
    "tsc-alias": "^1.8.16",
    "tsx": "^4.21.0",
    "typescript": "^5"
  }
}
